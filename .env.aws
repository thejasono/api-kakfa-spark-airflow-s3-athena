# AWS credentials for Spark containers. Populate with temporary or long-lived keys
# before starting Docker, or leave the values blank to rely on the default AWS
# credential provider chain (for example, when mounting ~/.aws).
AWS_REGION=eu-west-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_CREDENTIAL_EXPIRATION=
